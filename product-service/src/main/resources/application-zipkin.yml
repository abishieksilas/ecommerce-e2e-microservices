# Activate in IntelliJ: Active profiles = zipkin
# First start Zipkin on port 9411, then restart the service with this profile.
management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
